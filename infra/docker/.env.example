# Local development only. Compose works without copying this file.
# To override defaults, copy to infra/docker/.env (ignored by Git).
POSTGRES_DB=dallas_transit
POSTGRES_USER=dallas_dev
POSTGRES_PASSWORD=local_only_password
POSTGRES_PORT=5432
REDIS_PORT=6379
