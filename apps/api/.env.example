# Optional overrides; Node loads apps/api/.env in dev and start scripts.
HOST=127.0.0.1
PORT=3001

# Reserved for later storage integration; the scaffold does not consume these.
# Keep these aligned with infra/docker/.env if you customize credentials/ports.
DATABASE_URL=postgresql://dallas_dev:local_only_password@127.0.0.1:5432/dallas_transit
REDIS_URL=redis://127.0.0.1:6379
