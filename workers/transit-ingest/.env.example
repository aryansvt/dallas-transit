# Optional. Public local-development defaults only. Never commit .env.
DATABASE_URL=postgresql://dallas_dev:local_only_password@127.0.0.1:5432/dallas_transit
# Integration tests CREATE and DROP their own random database using this server.
# The connecting role needs CREATEDB. Never point this at production.
TEST_DATABASE_URL=postgresql://dallas_dev:local_only_password@127.0.0.1:5432/dallas_transit
# Optional operator-selected pedestrian provider; no public demo default.
# Complete HTTP(S) Valhalla /route URL, without embedded credentials/query string.
# WALKING_VALHALLA_URL=http://localhost:8002/route
# Production commands require explicit DATABASE_URL and disallow reset-static-data.
# Use Render internal URL in its shell, or external URL with ?sslmode=verify-full.
